#ifndef LV_CONF_H
#define LV_CONF_H

/*
    Minimal LVGL config for PlatformIO bring-up.

    For now, rely mostly on LVGL's defaults.
    Add feature-specific config here later as needed.
*/

#define LV_USE_LOG 0

#endif